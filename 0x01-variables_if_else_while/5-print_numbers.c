#include <stdio.h>

/**
 * main - program that prints the are lesser than 10
 * 
 *
 * Return: Always 0.
 */

int main(void)
{
int num;
while (num < 10)
{
printf("%d", num);
num += 1;
}
printf("\n");
return (0);
}
